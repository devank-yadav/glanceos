{
  "name": "@glanceos/screen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "size": "node -e \"const fs=require('fs'),zlib=require('zlib'),d='dist/assets';let m=0;for(const f of fs.readdirSync(d))if(f.endsWith('.js'))m=Math.max(m,zlib.gzipSync(fs.readFileSync(d+'/'+f)).length);const L=32000;console.log('screen JS gzip',m,'/ limit',L);if(m>L){console.error('FAIL: screen bundle over budget — keep the runtime tiny for e-ink/old TVs');process.exit(1)}\""
  },
  "dependencies": {
    "@glanceos/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.6"
  }
}
