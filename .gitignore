# dependencies
node_modules/

# builds
dist/
build/
*.tsbuildinfo
.vite/

# local state
.env
.env.*
!.env.example   # the setup reference — must ship with the repo
*.local
*.db
*.db-shm
*.db-wal
*.sqlite
apps/server/data/
coverage/

# OS noise
.DS_Store

# device artifacts
*.img
*.img.xz
.pio/
__pycache__/

# Showcase application drafts + poster source — personal submission material,
# not project documentation (and the seeded poster carries a ~2 MB editor payload).
docs/showcase/

# Internal build tracker — working directives, not documentation for anyone reading the repo.
docs/launch-roadmap.md
