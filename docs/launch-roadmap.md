# GlanceOS — Road to Launch (overnight build tracker v2)

**Directive (2026-06-24 night):** Owner is asleep; build autonomously through the night toward a real launch. **MODEL UPDATE (owner, latest):** launch as a **FREE, full-featured application — ALL features free at launch. Premium is deferred/undecided — do NOT build paywalls, billing, or feature-gating now.** Ship to **all platforms** (Android TV/Fire TV, Web/PWA/browser-TV, Apple TV/tvOS, Samsung Tizen, LG webOS, Raspberry Pi). Owner's explicit asks this round: **more integrations, more objects, polish every feature, add features you judge important, and finish Tracks A/C/D.** Limits reset **05:39**; a recovery cron resumes the loop.

**ROTATE across ALL tracks — don't starve any.** The open-ended tracks (E integrations, F objects/polish, H audit) must NOT crowd out the earlier launch work (A hardening, **C all-platforms**, D GTM). Each tick advances the **next track in this rotation that has a ready batch**, then records `LAST TRACK: X` in the MORNING SUMMARY so the following tick moves on:

> **rotation:** A → C → D → E → F → G → (then back to A) — with **H** (audit/bug-hunt) used only when the chosen track has nothing ready. This guarantees platforms (C) and GTM (D) keep getting built alongside integrations/objects/features. **Track B (paid multi-tenant/billing) stays DEFERRED** (free launch) — skip it.

**Operating rules (every batch = one commit):**
- Stay safe & reviewable: server+config typecheck clean + ALL tests green + `pnpm --filter @glanceos/screen size` ≤ 30000 gzip.
- Scrub every staged diff for AI/tool/vendor attribution and co-author trailers before committing — remove any; all credit to the owner.
- Migrations must be **additive + backfilled** (no data loss; existing single-tenant users get a default org). Update migration/test counts when touched.
- `zod` stays OUT of `apps/screen`; `billgenerator` repo stays pristine.
- Commit AND push per batch; tick the box here; keep the MORNING SUMMARY at the bottom updated.
- **If a batch needs human input** (paid dev account, live secret, DNS/host, real hardware) → build everything possible *without* the secret, gate it behind env, write a `NEEDS-YOU` note, and move on. **If a batch is too risky to do blind** (e.g. the full async Postgres rewrite) → do the safe scaffolding only, leave the risky part for human review, note it, move on.
- Verify live via Preview MCP when browser-observable.

The build loop drives the tracks roughly in this order, but always picks the next unchecked, unblocked, safe batch. **Track A first (fast, safe, model-agnostic), then interleave B (SaaS spine) + C (platforms) + D (GTM).**

---

## Track A — Launch hardening (model-agnostic; do first)
- [x] **A1 — Security holes.** ✅ `Secure` cookies on session/CSRF/share-unlock + `Strict-Transport-Security` header, both gated on `SECURE_COOKIES` (GLANCEOS_PUBLIC_URL=https… or GLANCEOS_SECURE_COOKIES=on) so local http still works; added `/api/public/*` IP limiter (240/min) + per-board-token `/unlock` limiter (12/min) for share-password brute-force defense; the three 500 handlers now return `{error:"internal error", ref}` (uuid, logged server-side) instead of echoing `e.message`. server tsc clean, 238 tests green.
- [x] **A2 — Backup/restore.** ✅ `src/backup-db.ts`: online `db.backup()` snapshots (consistent, safe while running) → `snapshotDatabase()` + `pruneSnapshots()` + CLI (`pnpm --filter @glanceos/server backup`). Optional in-process scheduled snapshots via `GLANCEOS_BACKUP_INTERVAL_HOURS`/`_KEEP`/`_DIR` (off by default). `docs/BACKUP.md` documents logical snapshots, full volume backup, and the stop→swap→start restore (incl. the secret-key caveat). +2 tests (valid queryable snapshot + retention prune). server tsc clean, 240 tests.
- [x] **A3 — QR claim deep-link.** ✅ New `claim.ts` (`parseClaimCode` + `PENDING_CLAIM_KEY`, sanitized) + 3 unit tests. `main.tsx` captures `?claim=<code>` on boot → stashes to sessionStorage, strips the query, lands on `#/screens`. ScreensPage consumes it (lazy `useState`), opens the Connect modal, and `ClaimForm` prefills + **auto-submits** (`initialCode`). Fixes the false "no typing URLs" promise. config tsc + 219 tests. Live-verified the capture (query stripped → `#/screens`, code stashed); the post-login modal-open step wasn't exercised live because the preview session is logged out, but the stashed code persists until login by design.
- [x] **A4 — PWA icons.** ✅ Generated real PNGs from `icon.svg` via sharp: `icon-192.png` + `icon-512.png` (alpha, purpose any), `icon-maskable-512.png` (512², **opaque full-bleed** for the platform mask, glyph well inside the safe zone), `apple-touch-icon.png` (180², **opaque** — iOS ignores SVG/alpha apple-touch). Wired into manifest.webmanifest (PNG any + maskable entries) + index.html (`icon` png + `apple-touch-icon` png). Verified: correct dims/opacity, manifest valid JSON, all served 200 image/png. Fixes the blank/letterboxed home-screen icon on installed PWAs.
- [x] **A5 — README + landing accuracy + unfurl** — **done, incl. server-injected absolute OG meta.** ✅ Fixed stale counts in README (85→**121** data sources/providers, 211→**213** block types, "all 199"→**213**, 399→**510+** tests) and on the landing ("Connect 85 apps"→**121**). ✅ Added a **GitHub repo link** twice on the landing (hero "View on GitHub ↗" CTA + footer link) — verified live logged-out (2 anchors render, calm monochrome). ✅ Added **OG/Twitter card meta** to `apps/config/index.html` (og:type/site_name/title/description/image + twitter summary_large_image) pointing at a baked **`apps/config/public/og.png`** (1200×630, sharp-rendered brand card — wordmark + glyph + tagline + "213 blocks · 153 templates · 121 integrations · MIT"); dev server serves `og.png` 200 and the tags appear in the served `/`. **✅ Server-injected absolute OG meta (this A tick):** a pure `apps/server/src/ogmeta.ts` `injectAbsoluteOg(html, publicUrl)` rewrites the SPA shell's `og:image`/`twitter:image` to absolute + adds `og:url`, applied once at boot in the `*` config-shell route **only when `GLANCEOS_PUBLIC_URL` is set** (idempotent; leaves already-absolute tags + skips when unset → relative `/og.png` still resolves for most scrapers). So strict link-unfurlers (and chat previews) get absolute URLs. **6 server tests** incl. one that runs the transform over the **real built `apps/config/dist/index.html`** (proves the regex matches the shipped tag format, not just a fixture); skips cleanly if the dist isn't built. server tsc + 254 tests; screen untouched. (Remaining nice-to-have: trimming the verbose README changelog blob — left intact, accurate.)
- [x] **A6 — Repo hygiene.** ✅ Added the community-health set so the repo reads as a real open-source project: **`CONTRIBUTING.md`** (quick start, repo-layout table, the four CI-enforced non-negotiables — zod-out-of-screen / ≤30 KB screen / additive schema / additive migrations — and how to add a provider/template; tone aligned to the README's "learning build, PRs mostly declined pre-1.0, forks welcome"), **`SECURITY.md`** (private disclosure via GitHub's "Report a vulnerability" — **no personal email committed** — plus the honest self-hosted threat model + out-of-scope HTTP-on-LAN), **`CHANGELOG.md`** (Keep-a-Changelog, dated from the actual tags v1→v9.7 + an Unreleased launch-hardening section), and `.github/` **issue forms** (bug_report.yml + feature_request.yml + config.yml routing security→advisories & questions→Discussions) + **PULL_REQUEST_TEMPLATE.md** (checklist mirroring the CI gates). README Contributing section now links all three. YAML issue forms validated; no workspace source touched (build/tests unaffected).
- [x] **A7 — Templates gallery search** ✅ a search box now sits beside the category chips on the Templates gallery, filtering all 159 starters + approved community boards by name + category + description (AND-semantics across terms), composing with the active chip, with a "No templates match…" empty-state. Search logic lives in a pure, unit-tested `apps/config/src/templateSearch.ts` (`templateMatches` / `filterTemplates`; 8 tests) so the auth-gated UI is verifiable; the input reuses the Integrations `.integ-search` styling. config tsc + 236 tests + production build clean. (Remaining A7 sub-ideas — "default sort to the most impressive boards" + first-run vibe-picker expansion — left as a follow-on: the gallery has no curated-rank signal yet and the vibe picker is a separate onboarding surface.)
- [x] **A8 — Keyless live-data starter templates.** ✅ Added a new **"Live & keyless"** gallery category with **6 templates** whose blocks ship a `source` pre-bound to a no-login provider, so "pick a template → live data, zero setup" works with no connection: **Hacker News Live** (`hackernews.top`), **Spaceflight News** (`spaceflightnews.articles`), **Earthquake Watch** (`usgs.quakes`), **On This Day** (`wikipedia.onthisday`), **Live Weather** (`openmeteo.current` stat + `openmeteoair.current` list), **Crypto Watch** (`coingecko.markets`). First starter templates to use `source` bindings; each `{kind, query, map}` mirrors the contract-tested `integrationObjects` maps, and all 7 bound `kind`s were verified present in the registry (so the server's existing resolver renders them live). Bound blocks keep a typed-in fallback string for offline/preview. starterTemplates schema test parses all docs (now **161**; the new category is populated); config tsc + **228 tests**. (Live resolution runs server-side via the proven `resolveWidgetData` path.)

## Track B — Multi-tenant SaaS + billing — ⏸️ DEFERRED (premium undecided; FREE launch)
> Owner switched to a **free, all-features-free launch**; premium is undecided. **Do NOT build billing, paid quotas, entitlement-gating, or the heavy Postgres rewrite now.** Skip Track B. (Kept here for when premium is decided. A lightweight free "workspaces/teams" feature, if wanted, would be a Track G item — without any paywall.)

- [ ] **B1 — Tenancy schema (migration, additive+backfilled).** `organizations` + `org_members(user_id, org_id, role)`; add nullable `org_id` to every tenant-scoped table (devices, layouts, tasks, queues, connections, uploads, fleet, signage, api_keys, automations, inlets, custom_data, shares, templates); backfill a default personal org per existing user and set org_id. Migration test asserts no data loss. **Keep all existing tests green.**
- [ ] **B2 — Org-scoped authz.** Principal resolver yields (user, activeOrg, role); ownership checks become org-scoped; `requireOrgRole` middleware; replace the single global first-user admin with per-org owner/admin. + tests.
- [ ] **B3 — Org lifecycle + UI.** Create org, invite members (signed token), accept, roles (owner/admin/member), switch active org, per-org settings. Config: org switcher + Members page.
- [ ] **B4 — Per-org gallery + registration/invites** (replace instance-global template gallery + single registration flag with per-org).
- [ ] **B5 — Billing (Stripe, env-gated).** customer+subscription+webhooks; plans (Free/Pro/Team); entitlement gating in middleware (limits: screens, members, integrations, templates); pricing page in config. Builds + tests with a stub; live keys = NEEDS-YOU.
- [ ] **B6 — Tenant ops.** Usage metering, per-org quotas, suspend/kill switch, minimal back-office; tighten abuse guards for hostile multi-tenant traffic (outbound fetch/inlet/oauth surface).
- [ ] **B7 — Postgres Store interface (RISKY — scaffold only tonight).** Define a `Store` interface over the data layer + opt-in Postgres backend behind env; SQLite stays default. The full "every call site async" migration is XL — do the interface/seam + a thin adapter + tests; **leave the full cutover for human review** (note it; do not break SQLite).

## Track C — All platforms, store-ready
- [x] **C1 — CI release pipeline.** ✅ `.github/workflows/release.yml`: on every `v*` tag (and on-demand `workflow_dispatch`) builds the debug-signed Android TV/Fire TV APK (`setup-java 17` + `setup-android` + `./gradlew assembleDebug`), stages `glanceos-androidtv.apk` + a sha256, uploads it as a run artifact, and on tags attaches it to a **GitHub Release** (softprops) with sideload + self-host instructions. No signing secrets (debug-signed). androidtv/README gains a "Download (prebuilt)" section. YAML validated (parses). Verify-by-construction: the Android build can't run in this CLI env (no Android SDK) — it runs on the owner's next tag/dispatch. NEEDS-YOU: Apple/Samsung/LG store builds still need vendor accounts (source-build boundary, documented). Follow-on: **C3** (runtime-configurable host) so the APK isn't pinned to the placeholder URL.
- [x] **C2 — tvOS Xcode project.** ✅ Committed `devices/tvos/project.yml` (XcodeGen): wires the 3 `Sources/` files + the hand-written `Info.plist` (`INFOPLIST_FILE` + `GENERATE_INFOPLIST_FILE=NO`, preserving the ATS LAN exception + full-screen), tvOS 17 target, placeholder bundle id. So Apple TV is now **one command** (`brew install xcodegen && cd devices/tvos && xcodegen generate`) like the other shells — no hand-assembled `.pbxproj`. Added `.gitignore` for the generated project; rewrote the README's "Generate the Xcode project" + caveats. YAML validated. NEEDS-YOU: Apple Developer account ($99) for on-device/App Store signing (Simulator needs none); a brand app-icon asset catalog (C4).
- [~] **C3 — Runtime-configurable host** — **done for the HTML webview shells (Tizen + webOS); native Android/tvOS = follow-on.** Both shells now ship a **first-run on-screen setup**: if no host is saved (and the baked `HOST` is still the placeholder), the TV shows a calm input — type your server address, press **Connect**, and it's stored in webview `localStorage`, so **no per-deployment rebuild**. A saved host always wins over the baked default; `?setup` re-opens the screen to change it; bare host/IP is normalised to `http://…` and trailing slashes stripped. Shared logic added to `devices/tizen/main.js` and `devices/webos/index.html`; READMEs updated. **Verified live in a headless browser for both shells** (temp Playwright check, since removed): first run → setup form shown (no redirect); enter host → saved + redirects to `<host>/screen/?tv=1…`; relaunch → auto-redirects with no setup. **Follow-on (native, can't verify blind):** Android TV reads the host from a build-time `BuildConfig` (documented); a runtime override there + the tvOS Swift shell are the next C3 slice.
- [~] **C4 — Real artwork for the shells** — **done for the webview shells; tvOS app-icon catalog is the remaining piece (pairs with the Apple-account NEEDS-YOU).** The shells shipped *off-brand placeholders* (Android used `#3B82F6` blue; webOS/Tizen icons were blank dark squares). Now all are the **GlanceOS brand mark** (`#191919`/`#fafafa`), generated from the one source `apps/config/public/icon.svg` via `sharp`: ✅ **webOS** `icon.png` 80 + `largeIcon.png` 130 (+ `appinfo.json` icon/bg colours → `#191919`); ✅ **Tizen** `icon.png` 512; ✅ **Android TV** a real **320×180 brand banner** at `drawable-xhdpi/banner.png` (glyph + wordmark lockup, verified) and the 4 adaptive-icon/banner **vectors recoloured** off the blue onto brand; READMEs updated (no longer "placeholder"). Each generated PNG was viewed to confirm the glyph renders. **Remaining (next C):** the **tvOS** `Assets.xcassets` App Icon & Top-Shelf brand-asset set — left for a tick where it can be Xcode-validated (a malformed brandassets `Contents.json` would only surface on the owner's first build; the Simulator runs without one today), and it's gated anyway on the Apple Developer account.
- [x] **C5 — Raspberry Pi flashable image** ✅ a complete **pi-gen custom stage** at `devices/pi-image/pi-gen/stage-glanceos/` so building a boot-to-dashboard SD `.img` is one command on a Linux host. It **copies the canonical `start-kiosk.sh` + `glanceos-kiosk.service`** the hand-install ships (single source of truth — the image can't drift from the documented path), installs the host-URL env file (bake via `GLANCEOS_URL=…`, editable post-flash), appends `consoleblank=0` to the kernel cmdline, drops a `getty@tty1` autologin (= raspi-config B2, written directly so it works on Lite), and `systemctl enable`s the kiosk + sets `graphical.target` (cage session, no display-manager). `00-packages` = cage/chromium-browser/unclutter; `EXPORT_IMAGE` → `deploy/*-glanceos-kiosk.img`. Shipped `config.example` + `pi-gen/README.md`; updated the parent README ("two paths, one launcher") + PLATFORMS.md. **Honesty: the pi-gen build NEEDS a Linux host (Docker/binfmt + root) — it cannot run in this macOS CLI / our CI, so it's reviewed (`bash -n` clean, executable) but NOT yet built/booted → stays Tier 2 "not verified on hardware."** Read-only SD overlay left as a noted follow-on. server+config tsc clean; screen 27421/30000 (untouched).
- [x] **C6 — Honest platform tiers.** ✅ New **`docs/PLATFORMS.md`**: a straight-story support matrix so nothing overclaims. **Tier 1** (recommended, CI-tested + live-verified): the **web/PWA** runtime in any browser + the multi-arch Docker server. **Tier 2** (build-from-source / sideload, each needs a platform SDK): Raspberry Pi kiosk, Android TV/Fire TV (APK on tag), Apple tvOS (Simulator now; device needs Apple acct), Samsung Tizen, LG webOS — all explicitly marked **"not yet verified on physical hardware"** (the web runtime they load *is* tested; the native wrappers are conventional but unproven on-device), with the per-platform NEEDS-YOU (SDK / dev account). **Tier 3** (experimental, not a launch claim): `esp32-eink` (scaffold-only polling path). A "what tested means" note is honest that there's no telemetry, so the matrix is hand-updated. Linked from `devices/README.md` (tier blockquote after the targets table) + the README repo map. Docs-only.
- [ ] **C7 — Org-aware pairing.** Devices belong to an org; claim assigns the display to the claimer's active org (depends on B1/B2).

## Track D — GTM / launch kit
- [~] **D1 — Visual kit** — **8 hero board screenshots done from the REAL runtime; the Studio editing GIF is the remaining piece.** Wrote a reusable renderer `apps/server/scripts/render-kit.mjs`: serves the built `apps/screen/dist` (honouring the `/screen/` base), drives **Playwright/Chromium** (already an apps/server dep) to post each starter-template board into the actual `?preview=1` runtime via the same `glanceos:state` message the Studio uses, screenshots at 1280×800@2×, and writes small palette PNGs to `docs/images/board-*.png`. Eight diverse boards (team agenda, café menu, big clock, weather, KPI, welcome sign, transit, markets — light **and** dark) now headline the README in a 2×4 gallery. Each render was viewed to confirm it painted real content (not blank). Truth, not mockups: it's the same <30 KB runtime that ships. **✅ Motion demo done (this D tick):** `apps/server/scripts/demo-motion.mjs` drives Playwright over the SAME real `?preview=1` runtime, posts a **multi-page board** (G2 rotation — page 1 = the `big-clock` template, page 2 = `cafe-menu-classic`, `pageRotateSeconds:3`), captures **32 frames** over ~8 s, and encodes with **ffmpeg** (2-pass palette) to **`docs/images/demo.gif` (130 KB, 32 frames)** + **`demo.mp4` (61 KB)**. Embedded as the README hero ("one board, several pages, rotating on the wall clock"). Truth, not a mock — the runtime that ships. **Verified:** extracted the t=1.5 s and t=4.5 s frames (page A live clock vs page B menu) — they differ (rotation fired) and both render rich static content (deliberately chose two no-live-data boards so there are no "weather unavailable" placeholders); GIF frame-count = 32. Doubles as a live re-verification of the G2 page rotation. **Still open:** an actual **Studio-*editing*** GIF (this is the board *runtime* in motion, which is the better hero anyway); the editor capture is auth-gated → wants a logged-in session.
- [x] **D2 — Positioning / comparison** — **reshaped for the FREE model** (no pricing/plans/hosted-tiers — that's deferred Track B). ✅ Added a **"How it's different"** section to the landing: a calm monochrome comparison table (CSS-checkmark on the GlanceOS column, no emoji) across 7 honest dimensions — Price (free, no subscription) / Where it runs (your server) / Your data (yours, zero telemetry) / Source (MIT, fork it) / Screens (any browser) / Make it yours (213 blocks + Notion-style editor) / Live data (137 sources, ~24 keyless). Framed vs **"typical hosted dashboard"** generically — no specific-competitor claims to get wrong or be unfair about. Also fixed the now-stale "Connect 121 apps" feature card → **137**. config tsc + 222 tests; **live-verified logged-out** (section renders, 7 rows, headers correct, screenshot confirms the table + checkmarks).
- [x] **D3 — Deploy config (one-command hosting).** ✅ `fly.toml` (Fly.io: prebuilt image + `/data` volume + `/ready` checks + always-on for SSE) and `render.yaml` (Render blueprint: image + disk + generated secret + trusted-proxy), plus `docs/DEPLOY.md` consolidating Compose / Fly / Render / any-Docker-host with the contract (port 8080 + `/data` volume + `GLANCEOS_PUBLIC_URL`) and the secret-persistence caveat; README "Host it online" pointer. render.yaml validated (parses); fly.toml standard schema. NEEDS-YOU: a host account + domain + DNS, and (for a public demo) setting `GLANCEOS_PUBLIC_URL` + a seeded read-only demo account.
- [x] **D4 — Launch kit.** ✅ `docs/LAUNCH.md`: ready-to-edit **Show HN** (title + body + first-comment), **r/selfhosted**, and **Product Hunt** (tagline/description/topics) drafts — all honest + accurate to current numbers (213 blocks · 153 templates · 109 integrations · 47 objects · MIT · one-container) and the FREE/premium-undecided framing; plus a **launch-day checklist** (cut a tag so the APK+image publish → verify the Release; 30-min stranger-self-host dry run; verify headline numbers; visual kit + hosted demo prep; repo hygiene; posting order/timing) and an honest "what to say if asked" Q&A. Pure docs.
- [x] **D5 — Press / media kit.** ✅ `docs/PRESS.md`: a one-page fact sheet for anyone writing about / posting GlanceOS — one-liner, short (~40w) + long (~90w) **boilerplate descriptions**, a **key-facts table** (MIT · free, no subscription · self-hosted one-container · **213 blocks / 159 templates / 153 integrations / 85 objects** · ≤30 KB runtime · stack), who-it's-for, links (repo/run/gallery/platforms/integrations/launch/architecture), an **asset index** reusing the existing `og.png` + `icon.svg` + the 8 `docs/images/board-*.png`, brand-use note (don't recolour), and an honest "a note on free" (launches fully free; premium undecided; nothing paywalled). All counts code-checked; every internal link verified to resolve. Linked from the README docs map. Pure docs. **GIF still pending:** tried synthesising an animated GIF via sharp from stacked frames — it didn't register as multi-page (sharp's raw→animated path), so a real GIF needs a dedicated encoder or an authed Studio capture; deferred rather than ship a broken asset.

- [x] **D6 — Launch-kit accuracy refresh.** ✅ The GTM copy people will actually post had drifted: `docs/LAUNCH.md` still said **109 integrations / 153 templates / 47 objects** and `docs/PRESS.md` said **153 integrations (~half keyless) / 85 objects / 150+ sources**. Refreshed both to the code-checked truth — **213 blocks · 159 templates (28 categories) · 165 integrations · 96 keyless · 96 objects** — across every Show HN / r/selfhosted / Product Hunt draft + the PRESS one-liner, short+long boilerplate and key-facts table ("over half need no login", "~96 keyless"). Counts verified by a Node type-strip import of `starterTemplates.ts` (159/28) + `registry.test` (165) + `integrationObjects.ts` (96). Pure docs (no TS touched → typecheck/tests/size unchanged from the prior green tick). (D1 Studio-editing GIF still the one open D item — needs a real GIF encoder / authed capture.)

## Track E — More integrations (free; server-side providers, zero screen cost)
Same proven pattern as the v9.7 build: each provider is a `reg({...})` in `apps/server/src/providers/registry.ts` (keyless first so it renders immediately; token/OAuth scaffolds light up on connect). Update `registry.test.ts` count + `docs/INTEGRATIONS.md` each batch. ~12–15 providers per batch.
- [x] **E1** — ✅ +12 **keyless** providers (85 → **97**): hackernews (news), wikipedia + dictionary + quotable (reference), frankfurter + binance (finance), iss + spaceflightnews (space), nager public-holidays (calendar), gutendex (books), freetogame (gaming), xkcd (fun). New CAT_LABELs: reference/space/fun. registry.test → 97; docs/INTEGRATIONS.md updated. server tsc + 240 tests; config tsc clean.
- [x] **E2** — ✅ +12 **keyless** providers (97 → **109**): themealdb + thecocktaildb + openbrewerydb (food), artic (art), spacex (space), coinpaprika (finance), f1 (sports), poetrydb + datamuse (reference), opentdb + dadjoke + uselessfacts (fun). New CAT_LABELs food/art. registry.test → 109; INTEGRATIONS.md updated. server tsc + 240 tests; config tsc clean.
- [x] **E3** — ✅ +12 **keyless** providers (109 → **121**, target met): nws (US weather alerts), eonet (NASA natural events), sunrise + zippopotam (place), itunes + deezer + musicbrainz (media), pokeapi + scryfall + opendota (gaming), catfact + chucknorris (fun). New CAT_LABELs weather/science. registry.test → 121; INTEGRATIONS.md updated. server tsc + 240 tests; config tsc clean. (Provider catalog now 121 — well past the 120+ target; further E batches optional.)
- [x] **E4** — ✅ +16 **keyless** providers (121 → **137**): gbif + openmeteoair + openmarine (science), coinlore + blockchaininfo + mempool (finance/crypto), adviceslip + officialjoke + affirmations (fun), worldtime (place), civilweather/7timer (weather), cheapshark game deals (gaming), espn + mlb scores (sports), firstorg countries + worldbank indicators (reference). **Built with an ultracode workflow**: 6 domain research agents (WebSearch/WebFetch-confirmed endpoints + real response shapes) → 18 candidates → per-candidate adversarial verify (keyless? unique id? valid category/shape? TS-compiles?) → 18/18 passed; I dropped 2 (firstcountries dup of firstorg; randomuser low-value) → 16 merged. registry.test → 137 (+ E4 assert block); **`docs/INTEGRATIONS.md` regenerated from the registry** (new `apps/server/scripts/gen-integrations-doc.mjs` parses `reg({...})` → fixes the stale "85" headline to the true **137** and the whole catalog, which had drifted since before E1). server tsc + 243 tests; screen gate 27048/30000.
- [x] **E5** — ✅ +16 **keyless** providers (137 → **153**): opensky + overpass (place: flights & nearby transit), aviationweather METAR + openmeteo (core global forecast) + openmeteoflood (weather), carbonintensity GB mix (science/energy), metmuseum + clevelandart + vam (art), nhl + openligadb (sports), openfoodfacts + fruityvice (food), boredapi + yesno + kanyerest (fun). Same ultracode research→adversarial-verify workflow: 6 domain agents → 17 candidates → per-candidate verify (keyless/unique/category/shape/TS-compiles) → 17/17 passed; dropped openpetfoodfacts (niche dup of OpenFoodFacts) → 16 merged. registry.test → 153 (+ E5 assert block); `docs/INTEGRATIONS.md` regenerated (84 keyless / 153 across 31 categories). server tsc + 243 tests; screen gate unchanged.
- [x] **E6** — ✅ +12 **keyless** providers (153 → **165**): bigdatacloud reverse-geocode + postcodesio + censusgeocoder (place/geo), policeuk crimes + fbiwanted + ukbills (civic/open-gov), agify + genderize + nationalize (fun name-novelty), noaatides (weather), usgswater streamflow (science), citypopulation (reference). Same research→adversarial-verify workflow: 6 domain agents → 18 candidates → 18/18 verified; I curated to 12 — dropped 3 dev sample-data (dummyjson/jsonplaceholder/dummyjsonquote — fake/lorem, not real dashboard sources), 2 of 3 `countriesnow.space` dupes (kept citypopulation), and **swpc** (its NOAA feed is an array-of-arrays, not the `{Kp}[]` the agent typed → would render empty; dropped over shipping a silent dud). registry.test → 165 (+ E6 assert block); `docs/INTEGRATIONS.md` regenerated (**96 keyless / 165** across 31 categories). server tsc + 243 tests; screen untouched.

## Track F — More objects + feature polish (free)
- [x] **F1** — ✅ +32 preset "objects" total → **57** covering every E1/E2/**E3** keyless provider (E3 batch: nws/eonet/sunrise/zippopotam/itunes/deezer/musicbrainz/pokeapi/scryfall/opendota — long-prose scalars catfact/chucknorris skipped). All 9 integrationObjects tests green (schema + transform contracts hold for all 57). Originally +22 (25→47) for E1/E2 keyless (HN, Wikipedia, FX, ISS, spaceflight, holidays, Gutenberg, dictionary, quotes, xkcd, free games, Binance, recipes, cocktails, SpaceX, Coinpaprika, art, poetry, trivia, words, breweries, F1) — each a one-click `+` chip on Connections that copies a pre-bound block to the Studio clipboard. Long-prose scalars (dadjoke/uselessfacts) skipped (don't bind cleanly to a stat). All 9 integrationObjects tests green (schema-validation + transform-contract hold for all 47); config tsc + 219 tests. (More object batches can follow under F for token/OAuth providers.)
- [x] **F (objects b3)** — ✅ +13 preset "objects" for the **E4** keyless providers → **70** total: gbif species, open-meteo air + marine (bulletList), coinlore top, civilweather forecast, cheapshark deals, espn + mlb scores, firstorg countries, worldbank indicator (bulletList); blockchaininfo BTC price, mempool fee, worldtime (stat). Long-prose scalars (adviceslip/affirmations/officialjoke) skipped — don't bind cleanly to a stat (same call as dadjoke/catfact). All E4 providers' categories already had CAT_LABELs (no integrations.tsx change). All 9 integrationObjects tests green (schema-validation + transform-contract hold for all 70); config tsc + 219 tests.
- [x] **F (objects b4)** — ✅ +15 preset "objects" for the **E5** keyless providers → **85** total: openmeteo "weather now" + aviationweather METAR + yesno (stat); opensky aircraft, overpass stations, openmeteoflood discharge, carbonintensity GB mix, metmuseum/clevelandart/vam art search, nhl standings, openligadb table, openfoodfacts, fruityvice, boredapi activities (bulletList). Skipped the long-prose scalar (kanyerest quote) — same call as adviceslip/dadjoke. Field maps verified against each resolver's real item keys (title/label/value). All 9 integrationObjects tests green (contract holds for all 85); config tsc + 222 tests.
- [x] **E7** — ✅ +12 **keyless** providers (165 → **177**): astros people-in-space (space), openmeteogeo city-search + ipwhois IP-geolocation + nominatim geocoder (place), tfl London line-status + citybikes bike-share networks (**new `transit` category**), wikitrends most-read articles (reference), openmeteoelev elevation (science), exchangeapi currency/crypto rates (finance), rickandmorty characters (fun), metno MET-Norway forecast (weather), openfda food recalls (civic). **Every endpoint + JSON shape confirmed live by curl before writing the resolver** (no research-agent guesses): dropped numbersapi (404), googlebooks (shared-quota 429), reliefweb (v1 410) over shipping duds; swapped ip-api→ipwho.is for HTTPS. New CAT_LABEL `transit: "Transit & mobility"`. registry.test → 177 (+ E7 assert block); `docs/INTEGRATIONS.md` regenerated (**108 keyless / 177** across **32** categories). server tsc + config tsc + 243 server / 236 config tests; screen 27421/30000. **Note:** the marketing headlines (landing/og/README/PRESS/LAUNCH) still read 165/96 — a one-batch lag to re-sync to 177/108/32 in the next D/H accuracy tick. *(This E7 entry sat in Track F due to a prior mis-merged Edit — labelled E7 so it reads correctly; the morning-summary line is canonical.)*
- [x] **E8** — ✅ +11 **keyless** providers (177 → **188**): kraken crypto ticker + bitpay BTC-rates + feargreed Fear-&-Greed index (finance), launchlibrary upcoming rocket launches (space), nobel laureates + openalex most-cited works (reference), openverse openly-licensed image search (media), inaturalist nature observations (science), urbandictionary slang + disney characters + superhero stats (fun). **Every endpoint + JSON shape curl-confirmed before the resolver** (incl. the nested `nobelPrizes[].laureates[].fullName.en`, launchlibrary `name/net/provider`, inaturalist defensive `species_guess‖taxon.preferred_common_name‖taxon.name`). Probed ~16, curated to 11 — dropped solarsystem (now key-gated), coinbase (id clash with the OAuth scaffold), fishwatch (empty body), datausa (returns HTML), transport.rest (empty), wttr (weather already 6-deep), swapi (name-only, weakest). All existing categories → no integrations.tsx change. registry.test → 188 (+ E8 assert block); `docs/INTEGRATIONS.md` regenerated (**119 keyless / 188** across 32 categories). server tsc + 254 tests; screen untouched. **Note:** marketing headlines now lag at 177/108/32 → re-sync to 188/119 next D/H accuracy tick (with the still-pending objects sync).
- [x] **F (objects b5)** — ✅ +11 preset objects for the **E6** keyless providers → **96** total: bigdatacloud "where am I" + noaatides + agify + genderize (stat); policeuk crimes, fbiwanted, ukbills, nationalize, postcodesio, usgswater streamflow, citypopulation (bulletList). Skipped `censusgeocoder` (an address-geocoding utility, not a glanceable feed). Field maps verified against each resolver's real item keys. All 9 integrationObjects tests green (contract holds for all 96); config tsc + 228 tests.
- [x] **F (objects b6)** — ✅ +8 preset objects for the **E7** keyless providers → **104** total: metno "weather now" (stat); astros people-in-space, tfl tube line-status, citybikes networks, wikitrends most-read, exchangeapi FX/crypto rates, rickandmorty characters, openfda food recalls (bulletList). Skipped the 4 utility/lookup E7 providers (openmeteogeo / ipwhois / nominatim / openmeteoelev — geocoders & one-shot lookups, not glance tiles, same call as censusgeocoder). Field maps verified against each E7 resolver's real item keys (title/label/value). All 9 integrationObjects tests green (contract holds for all 104); config tsc + 236 tests.
- [ ] **F2** — Studio/editor polish: rough edges + small UX wins (search where missing, default sorts, empty/error/loading states).
- [ ] **F3** — App-wide consistency & a11y sweep (light+dark): focus states, keyboard-shortcuts help, toasts, error states, mobile.

## Track G — Important new features (owner: "add features you judge important"; all FREE)
- [x] **G1** — Board version history / restore — **server + Studio UI both done.** Server (prior tick): migration `023_layout_versions` (additive, empty-start, `ON DELETE CASCADE`); `updateLayout` archives the document it replaces (throttled `GLANCEOS_VERSION_MIN_INTERVAL_MS`, default 5 min; pruned to newest `GLANCEOS_VERSION_KEEP`, default 50); `snapshotLayout`/`listLayoutVersions`/`getLayoutVersionDocument`; API `GET /api/layouts/:id/versions` + `POST /api/layouts/:id/versions/:vid/restore` (owner-only; restore archives the pre-restore state too → itself undoable, then pushes to screens); +3 server tests. **UI (this tick):** a **Version history** button (new `Icon.history` clock-rewind glyph) in the Studio topbar opens a popover listing versions newest-first with a relative-time label (new pure `timeAgo()` helper + 3 unit tests) and a per-row **Restore** (busy/empty/loading states); restoring POSTs, re-fetches the board, `dispatch({type:"replace"})` reloads the editor, and toasts. New CSS `.history-list/.history-row/.history-when`. config tsc + **222 tests** + build smoke green. (Auth-gated → verified via tsc/build/unit test, not a logged-out click-through.)
- [~] **G2** — Multi-page boards + rotation — **schema + screen runtime done; Studio authoring UI is the next G batch.** ✅ Schema: optional additive `pages?: Row[][]` (max 8 extra pages; page 0 = `rows`) + `pageRotateSeconds?` (3–3600) on `Layout` — no migration; old docs + screens that ignore it are unchanged. ✅ Screen runtime: `renderPayload` derives the active page from `[rows, ...pages]` by the wall clock (`activePageIndex`, deterministic → screens stay in lockstep) and renders it as a normal board; a 1 s ticker (`armPageRotate`, cleaned up like `spotlightStop`) re-renders on page change (sig differs → clean rebuild). zod stays out of screen (type-only). **Verified:** schema 16 tests (round-trip + bounds), screen 53 tests (incl. `activePageIndex` math + a real-`renderPayload` PAGEA→PAGEB-on-clock-advance test), **screen gzip 27421/30000** (+373 B), and a **live Playwright check** posting a 3 s-rotating 2-page board to the real `?preview=1` runtime confirmed it rotates between pages over the wall clock. **Next (deferred, auth-gated):** the Studio **page-lens** authoring UI (a pages strip: add/dup/delete/reorder pages + a "rotate every N s" control) — left for a tick where the editor can be live-verified.
- [x] **G3** — ✅ ⌘K command palette is now a real **global search**: it indexes the user's **boards** (fetched from `/api/layouts` → "Open <name>" jumps straight to `/edit/:id`) and the full **153-template gallery** (type a name → opens the gallery), plus a new **Data inlets** nav entry. Boards refresh on auth + route change. config tsc + 219 tests; app boots clean (live smoke). (Follow-on: deep-link a template to its preview; integrations/blocks in the palette if wanted.)
- [x] **G4** — ✅ **`timeWindow` automation condition** — gate any automation to a daily time range in the user's timezone (e.g. "only during office hours", "only overnight"). Additive condition `{ type: "timeWindow", startMin, endMin, daysMask? }` (minute-of-day; **wraps past midnight** when end ≤ start; `daysMask` = bit-per-weekday, default every day). A pure clock gate — references no data, fetches nothing. Schema union + zod (bounds + `daysMask` default 127); engine `evaluate()` case (pure/total, reads only `ctx.time`); the tree-walk helpers (refsField/trend/stale/sustained) no-op on it. Builder: a **"+ between hours"** button + a BETWEEN node (two time inputs + the same weekday-toggle row the time/sun triggers use), preserved through `norm()` like `sustained`. Tests: **+5** — 4 engine cases (in-window/end-exclusive, midnight-wrap, weekday-mask, default-all-days) + 1 zod validate/default/bounds; server 248, schema 16, config 236; screen untouched (no automation import). Builder create-UI is tsc-checked (auth-gated → not click-verified).
- [ ] **G5** — (loop adds more as the audit surfaces them.)

## Track H — Continuous audit & bug-hunt (NEVER "done" — owner: "don't stop, keep finding & fixing")
The loop must not idle. When A/C/D/E/F/G have no ready batch, run an **audit/bug-hunt** pass (typecheck strictness, dead code, broken/edge UX, a11y, perf, security, doc accuracy, stale claims, live Preview MCP smoke of key flows) → write the findings as new checkboxes under the relevant track → fix the top one this tick. Keep the tree green; keep shipping. This track is intentionally inexhaustible.
- [ ] **H-ongoing** — each idle tick: audit → log findings here → fix one → commit.
- [x] **H2 — Launch-count accuracy re-sync (177/108/104).** ✅ Cleared the standing one-batch lag (flagged after E7→177 providers + F-b6→104 objects): the marketing surfaces still read **165 integrations / 96 keyless / 96 objects**. Re-synced every public instance to the code-checked truth — **213 blocks · 159 templates · 177 integrations · ~108 keyless · 104 objects · 550+ tests · 32 integration-categories**: landing card ("Connect 165→**177** apps", "159→**171** more") + comparison ("165 sources/~96"→"**177**/~**108**"); `index.html` og:description (165→177); README (gallery caption, status headline + "165 providers across 31 cat"→"**177** across **32**" + "~96 no-login"→"~**108**" + 540+→**550+** tests); PRESS short/long + facts table (165→177, ~96→~108 keyless, 96→**104** objects); LAUNCH all 4 drafts + headline; **regenerated `og.png`** (213 · 159 · **177** · MIT — viewed). config tsc + 236 tests; **landing live-verified logged-out** (Connect 177 / 177 sources, zero stale 165/96). Ran this (Track-C slot) because C had no safely-buildable batch — tvOS asset-catalog + native host-override are NEEDS-Apple / can't-compile-blind.
- [x] **H1 — Launch-count accuracy sweep.** ✅ G's only open item (the G2 Studio page-lens authoring UI) is auth-gated + the v9.0-flagged highest-risk refactor → not safe to do blind, so this G-slot tick ran an audit instead. Found the headline counts had drifted stale as E4–E6 + A8 grew them, and fixed every public-facing instance to the true totals (**213 blocks · 159 templates · 165 integrations · 96 keyless · 540+ tests**): landing feature card ("Connect 137→**165** apps", "131→**159** more"), landing "How it's different" row ("137 sources, ~24"→"**165** sources, ~**96**"), `index.html` og:description (153 templates→159, 121 integrations→165), README (gallery caption 153/121→159/165; status headline + "121 providers across 29 cat"→"**165** across **31**"; "~24 no-login"→"~**96**"; "510+ tests"→"**540+**"), and **regenerated `og.png`** ("213 blocks · 159 templates · 165 integrations · MIT" — viewed to confirm). config tsc + 228 tests; **landing live-verified logged-out** (Connect 165 / 165 sources, zero stale 137/121). **Open (deferred, not blind-safe):** G2 Studio page-lens authoring UI — needs an authed editor session to build + verify.

## NEEDS-YOU (cannot be done autonomously — queued for the owner)
- Apple Developer account ($99/yr) → tvOS TestFlight/App Store.
- Samsung & LG developer accounts / TV dev-mode certs → Tizen/webOS store submission.
- Stripe account + live keys → real billing (B5 builds against a stub meanwhile).
- A hosting target + domain + secrets → hosted SaaS / public demo (D3 preps the config).
- Real hardware (Fire TV, Samsung, LG, Apple TV, Pi) → on-device test matrix (C6).

---

## MORNING SUMMARY
*(updated each batch by the build loop)*
- Audit complete (12-agent recon, cross-verified): core product is launch-grade; gaps are packaging, polish, and the (now-chosen) SaaS spine. This roadmap is the plan.
- **A1 ✅** security holes (Secure cookies+HSTS TLS-gated, public/unlock rate limits, opaque 500s). server 238 tests.
- **A2 ✅** backup/restore (online snapshots + CLI + optional scheduler + docs/BACKUP.md). server 240 tests.
- **MODEL CHANGED** → FREE launch, all features free, premium deferred. Track B (billing/multi-tenant) DEFERRED. New tracks E (integrations) / F (objects+polish) / G (features) / H (never-stop audit). Cron rebuilt to never idle.
- **E1 ✅** +12 keyless providers → **97** (HN, Wikipedia, dictionary, quotable, Frankfurter FX, Binance, ISS, Spaceflight News, holidays, Gutenberg, FreeToGame, xkcd). server 240 tests.
- Loop set to **ROTATE A→C→D→E→F→G (+H filler)** so platforms/GTM aren't starved (cron 27460db3).
- **A3 ✅** QR claim deep-link (scan → prefill + auto-submit claim; capture live-verified). config 219 tests.
- **C1 ✅** release workflow → builds + attaches the Android TV/Fire TV APK to GitHub Releases on each tag (no secrets); androidtv README "Download" section. (CI build runs on the owner's next tag.)
- **D3 ✅** one-command hosting: fly.toml + render.yaml + docs/DEPLOY.md (Compose/Fly/Render/any-Docker) + README pointer. (Actual deploy/domain = NEEDS-YOU.)
- **E2 ✅** +12 keyless providers → **109** (TheMealDB, TheCocktailDB, Open Brewery, Art Institute, SpaceX, Coinpaprika, F1, PoetryDB, Datamuse, Open Trivia, Dad Jokes, Random Facts). server 240 tests.
- **F1 ✅** +22 one-click preset objects → **47** (every E1/E2 keyless provider). config 219 tests; all schema/transform contracts hold.
- **G3 ✅** ⌘K global search — jump to any board by name + search the 153 templates + Data inlets nav. config 219 tests.
- **A4 ✅** real PNG PWA icons (192/512 + opaque maskable + opaque apple-touch) from the brand SVG; manifest + index wired; all served 200. Fixes blank installed-app icon.
- **C2 ✅** tvOS XcodeGen `project.yml` → Apple TV is one-command (`xcodegen generate`) like the other shells; README + .gitignore updated. (On-device signing = Apple acct NEEDS-YOU.)
- **D4 ✅** launch kit (docs/LAUNCH.md): Show HN / r/selfhosted / Product Hunt drafts + launch-day checklist + honest Q&A.
- **E3 ✅** +12 keyless providers → **121** (NWS alerts, NASA EONET, sunrise, zip lookup, iTunes, Deezer, MusicBrainz, PokéAPI, Scryfall, OpenDota, cat facts, Chuck Norris). server 240 tests.
- **F (objects b2) ✅** +10 preset objects for the E3 list providers → **57** total. config 219 tests; all schema/transform contracts hold.
- **G1 (server) ✅** board version history foundation — migration 023 + capture-on-save (throttled/pruned) + list/restore API + 3 tests. server 243 tests. (Studio UI = next G batch.)
- **A5 ✅** launch accuracy + social unfurl — fixed stale counts (README + landing now read 121 integrations / 213 blocks / 153 templates / 510+ tests), added GitHub links on the landing (hero CTA + footer, live-verified), and OG/Twitter card meta + a baked 1200×630 `og.png` brand card. config tsc + 219 tests; landing verified logged-out. (Deferred: server-injected absolute og: URL.)
- **C4 ✅ (webview shells)** real brand artwork — the shells shipped off-brand placeholders (Android blue, blank webOS/Tizen squares); now all rendered from the one source `icon.svg` via sharp: webOS 80/130 + appinfo colours, Tizen 512, Android TV 320×180 brand banner + recoloured adaptive vectors. Every PNG viewed to confirm the glyph. READMEs de-"placeholder"-ed. Screen gate 27048/30000; only `devices/*` touched. (tvOS app-icon xcassets = next C, needs Xcode validation + Apple acct.)
- **D1 ✅ (screenshots)** visual kit — new `apps/server/scripts/render-kit.mjs` drives Playwright over the REAL `?preview=1` screen runtime to render 8 starter-template boards (light+dark) → `docs/images/board-*.png`, now a 2×4 gallery atop the README. Each verified painted (not blank). server tsc clean; `dist/` not committed. (Studio-editing GIF = remaining D piece.)
- **E4 ✅** +16 keyless providers → **137** (science: gbif/open-meteo air+marine; crypto: coinlore/blockchain.info/mempool; fun: advice/jokes/affirmations; place: worldtime; weather: 7timer; gaming: cheapshark; sports: espn/mlb; reference: first.org/worldbank). Ultracode workflow (research→adversarial-verify, 18/18 passed, 2 dropped). registry.test → 137; INTEGRATIONS.md **regenerated from the registry** (fixed the stale 85→137 headline + whole catalog). server 243 tests; screen 27048/30000.
- **F (objects b3) ✅** +13 preset objects for the E4 keyless providers → **70** total (air/marine/crypto/forecast/deals/scores/countries/worldbank/time). config 219 tests; all schema/transform contracts hold.
- **G1 (Studio UI) ✅** Version-history panel in the editor topbar (new Icon.history) → lists versions newest-first with a relative-time label (new tested `timeAgo`) + per-row Restore wired to the G1 endpoints; restore reloads the editor + toasts. config tsc + 222 tests + config build smoke. **G1 now fully done** (server + UI).
- **A6 ✅** repo hygiene — added CONTRIBUTING.md, SECURITY.md (GitHub private advisories; no email committed), CHANGELOG.md (tag-dated), `.github/` issue forms + PR template; README links them. YAML validated; docs-only (no source touched).
- **C6 ✅** honest platform tiers — new `docs/PLATFORMS.md` (Tier 1 web/PWA+Docker, CI-tested; Tier 2 native shells, build-from-source + "not yet hardware-verified" + per-platform NEEDS-YOU; Tier 3 esp32 experimental/scaffold-only). Linked from devices/README + README. Docs-only.
- **D2 ✅** landing "How it's different" — honest 7-row comparison table vs "typical hosted dashboard" (free/self-hosted/your-data/MIT/any-screen/213-blocks/137-sources), CSS-checkmark monochrome, no competitor names; fixed the stale 121→137 apps card. config 222 tests; live-verified logged-out (screenshot).
- **E5 ✅** +16 keyless providers → **153** (opensky/overpass, aviationweather/open-meteo forecast+flood, UK carbon intensity, Met/Cleveland/V&A museums, NHL/OpenLigaDB, OpenFoodFacts/Fruityvice, bored/yesno/kanye). Ultracode workflow (17/17, dropped 1). registry.test → 153; INTEGRATIONS.md regenerated (84 keyless). server 243 tests.
- **F (objects b4) ✅** +15 preset objects for the E5 keyless providers → **85** total (weather-now/METAR/yesno stats; aircraft/stations/flood/energy-mix/3 museums/NHL/Bundesliga/food/fruit/bored lists; skipped kanyerest prose). config 222 tests; contract holds for all 85.
- **G2 (schema + screen) ✅** multi-page boards — additive optional `pages?`/`pageRotateSeconds?` on Layout (no migration) + screen runtime renders the wall-clock-active page with a cleanup-managed rotation ticker. schema 16 + screen 53 tests; screen gzip 27421/30000; **live Playwright proof** of rotation. (Studio page-lens authoring UI = next G batch, auth-gated.)
- **A8 ✅** keyless live-data starter templates — new "Live & keyless" category, 6 templates pre-bound to no-login providers (HN, spaceflight, USGS quakes, Wikipedia on-this-day, Open-Meteo weather+air, CoinGecko) → "pick a template → live data, zero setup." First templates using `source`; bound kinds verified in the registry. config 228 tests (starterTemplates parses all 161).
- **C3 (HTML shells) ✅** runtime-configurable host — Tizen + webOS shells get a first-run on-screen setup that saves the server to localStorage (no per-deployment rebuild; `?setup` to change; saved host wins over the baked default). Live-verified both shells headlessly (setup→save→redirect→auto-redirect). READMEs updated. (Android/tvOS native host override = follow-on.)
- **D5 ✅** press/media kit — new `docs/PRESS.md` (fact sheet: one-liner, short/long boilerplate, key-facts table, links, asset index reusing og.png + board screenshots, honest "free" note). All counts code-checked; links verified; README docs map updated. (Motion GIF still pending — sharp animated-GIF synth didn't work; needs a real encoder/authed capture.)
- **E6 ✅** +12 keyless providers → **165** (geo: bigdatacloud/postcodes.io/census; civic: UK police/FBI wanted/UK bills; fun: agify/genderize/nationalize; noaa tides; usgs water; city population). Workflow 18/18; curated to 12 (dropped 3 sample-data, 2 countriesnow dupes, swpc wrong-shape). registry.test → 165; INTEGRATIONS.md regenerated (96 keyless). server 243 tests.
- **F (objects b5) ✅** +11 preset objects for the E6 keyless providers → **96** total (where-am-I/tide/agify/genderize stats; UK police/FBI/UK bills/name-origins/UK postcodes/USGS streamflow/city populations lists; skipped censusgeocoder utility). config 228 tests; contract holds for all 96.
- **H1 ✅ (G-slot)** launch-count accuracy sweep — G's only item (Studio page-lens) is auth-gated + highest-risk-blind, so ran an audit instead: fixed stale counts everywhere (landing card/comparison, og:description, README, regenerated og.png) to the true 213 blocks / 159 templates / 165 integrations / 96 keyless / 540+ tests. config 228 tests; landing live-verified. (G2 editor UI stays open for an authed tick.)
- **A7 ✅** Templates gallery search — a search box beside the category chips filters all 159 starters + community boards by name/category/description (AND across terms), composing with the active chip + a no-match empty-state. Logic in a pure unit-tested `templateSearch.ts` (8 tests). config tsc + 236 tests + build clean. (Curated default-sort + vibe-picker expansion deferred — no rank signal yet.)
- **C5 ✅** Raspberry Pi flashable image — a pi-gen custom stage (`devices/pi-image/pi-gen/`) that bakes the kiosk into an SD `.img`, reusing the SAME launcher + systemd unit as the hand-install (no drift): host-URL env + no-blank cmdline + tty1 autologin + enabled service + graphical default; `config.example` + README; PLATFORMS.md updated. NEEDS a Linux host to build (can't run in this CLI/CI) → reviewed (`bash -n` clean) but not yet booted → Tier 2. server+config tsc clean; screen 27421/30000.
- **D6 ✅ (GTM)** launch-kit accuracy refresh — LAUNCH.md + PRESS.md (the Show HN / r/selfhosted / Product Hunt / press-sheet copy) were stale (109 integ / 153 templates / 47–85 objects); refreshed both to the code-checked 213 blocks · 159 templates (28 cat) · 165 integrations · 96 keyless · 96 objects. Counts verified by Node import of the source. Pure docs. (D1 GIF still open — needs an encoder.)
- **E7 ✅** +12 keyless providers (165 → **177**): astros (space); openmeteogeo/ipwhois/nominatim (place); tfl/citybikes (**new transit category**); wikitrends (reference); openmeteoelev (science); exchangeapi (finance); rickandmorty (fun); metno (weather); openfda (civic). Every shape curl-confirmed before coding (dropped numbersapi/googlebooks/reliefweb as broken). registry.test → 177; INTEGRATIONS.md regenerated (108 keyless / 32 cats). server 243 + config 236 tests; screen 27421/30000. Marketing headlines lag at 165/96 → re-sync next D/H tick.
- **F (objects b6) ✅** +8 preset objects for the E7 keyless providers → **104** total (metno weather-now stat; astros/tfl/citybikes/wikitrends/exchangeapi/rickandmorty/openfda lists; skipped the 4 geocoder/lookup utilities). config 236 tests; contract holds for all 104. Also repaired a roadmap-doc corruption from the E7 tick (the F-b5 checkbox head had been clobbered + its tail left dangling on the E7 line — split back apart).
- **G4 ✅** new `timeWindow` automation condition — gate any rule to a daily time range (timezone-aware, wraps past midnight, optional weekday mask). Schema union + engine `evaluate()` (pure clock gate) + builder "+ between hours" node (preserved via norm() like sustained). +5 tests (4 engine + 1 zod). server 248 / schema 16 / config 236; screen untouched. (Chose this over the auth-gated G2 page-lens editor — verifiable end-to-end via unit tests.)
- **A5-finish ✅** server-injected absolute OG meta — pure `ogmeta.ts injectAbsoluteOg()` rewrites the SPA shell's og:image/twitter:image to absolute + adds og:url, applied at boot in the config-shell route only when `GLANCEOS_PUBLIC_URL` is set (idempotent; relative still works when unset). Closes the last open A item (A5 now fully ✅). 6 tests incl. one over the real built shell; server 254; screen untouched.
- **H2 ✅ (C-slot)** launch-count accuracy re-sync — Track C had no safely-buildable batch (tvOS asset-catalog needs Apple + Xcode; native host-override is can't-compile-blind Kotlin/Swift), so cleared the standing marketing-count lag: every public surface (landing card + comparison, og:description, README, PRESS, LAUNCH, regenerated og.png) now reads the true **213 blocks · 159 templates · 177 integrations · ~108 keyless · 104 objects · 550+ tests**. config 236 tests; landing live-verified (177/~108, zero stale 165/96).
- **D1-motion ✅** demo GIF/MP4 — `demo-motion.mjs` captures the real <30 KB runtime rotating a multi-page board (big-clock ↔ café menu, G2 wall-clock rotation) via Playwright → ffmpeg 2-pass palette → `docs/images/demo.gif` (130 KB/32 frames) + `demo.mp4` (61 KB), now the README hero. Verified frames at t=1.5 s/4.5 s differ (rotation fired) + both render rich; doubles as a G2 live re-verify. (Studio-*editing* GIF still wants an authed session — runtime-in-motion is the better hero regardless.)
- **E8 ✅** +11 keyless providers (177 → **188**): kraken/bitpay/feargreed (finance), launchlibrary (space), nobel/openalex (reference), openverse (media), inaturalist (science), urbandictionary/disney/superhero (fun). Every shape curl-confirmed; probed ~16, dropped 5 broken/keyed + coinbase id-clash + swapi/wttr to curate. registry.test → 188; INTEGRATIONS.md regenerated (119 keyless / 32 cats). server 254 tests; screen untouched. Marketing headlines lag at 177/108 → re-sync to 188/119 next D/H.
- LAST TRACK: E
